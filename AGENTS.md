# 项目级原则

## 模型评估必须可视化

- 凡是涉及模型评估的分析、实验、报告或结论，都必须生成并提供图表，直观展示预测结果与实际结果的关系。
- 图表应至少覆盖预测值和真实值；根据任务特性补充误差、方向命中、置信区间、分组表现或其他能说明模型行为的可视化。
- 仅报告数值指标不足以完成模型评估。若数据、运行环境或其他限制使绘图暂时不可行，必须明确记录原因、缺失的图表以及补绘计划，不能将评估标记为完成。

## 实施方案版本管理

- 当实施方案发生重大变更，或已有证据证实当前路径不可行时，递增方案主版本号，例如从 `V3` 变为 `V4`。
- 在同一主方案内进行的小范围、可回退的迭代，使用 `phase` 编号，而不变更主版本号，例如 `V3 phase 2`。
- 每份实验计划、运行记录和结果报告都应写明当前方案版本与 phase；主版本变更时，记录变更原因、被替代的路径，以及支持该决定的证据。
